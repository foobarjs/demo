{
  "name": "foobarjs-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "foobar serve --dev",
    "start": "foobar serve",
    "test": "foobar test",
    "db:migrate": "foobar db migrate",
    "db:seed": "foobar db seed",
    "db:fresh": "foobar db fresh",
    "db:reset": "foobar db reset"
  },
  "dependencies": {
    "esbuild": "^0.25.0",
    "foobarjs": "file:../framework"
  }
}
